import intialState from "./initialState";
import * as types from "../actions/types";

const stakeReducer = (state = intialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default stakeReducer;
