import intialState from "./initialState";
import * as types from "../actions/types";

const widrawReducer = (state = intialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default widrawReducer;
